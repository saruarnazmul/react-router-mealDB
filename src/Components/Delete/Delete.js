import React from 'react';

const Delete = (props) => {
    // console.log(props.meal);
    // const { idMeal } = props.meal
    return (
        <div>
            {/* <h2>{props.meal.idMeal}</h2>
            <h2>hello </h2> */}
        </div>
    );
};

export default Delete;